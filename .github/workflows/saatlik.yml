name: Demo Botlari (XAU/XAG + AUDNZD) - 15dk

on:
  schedule:
    # 15 dakikada bir ama TUHAF dakikalarda (7/22/37/52), yuvarlak
    # dakikalarda DEGIL. GitHub'in zamanlayicisi :00/:15/:30/:45
    # slotlarinda asiri yuklenir (herkes oraya program yazar) ve bu
    # slotlardaki isler ciddi gecikir ya da tamamen atlanir - "*/15" ile
    # denendiginde 35 dakikada 3 tetikleme ust uste kacirildi. Yogun
    # olmayan dakikalara kaydirmak, GitHub'in kendi onerdigi cozumdur.
    - cron: "7,22,37,52 * * * *"
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  calistir:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Bagimliliklari kur
        run: pip install -r requirements.txt

      - name: XAUUSD botunu calistir
        continue-on-error: true
        env:
          METAAPI_TOKEN: ${{ secrets.METAAPI_TOKEN }}
          MT5_LOGIN: ${{ secrets.MT5_LOGIN }}
          MT5_PASSWORD: ${{ secrets.MT5_PASSWORD }}
          MT5_SERVER: ${{ secrets.MT5_SERVER }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: python xau_main.py

      - name: XAGUSD botunu calistir
        continue-on-error: true
        env:
          METAAPI_TOKEN: ${{ secrets.METAAPI_TOKEN }}
          MT5_LOGIN: ${{ secrets.MT5_LOGIN }}
          MT5_PASSWORD: ${{ secrets.MT5_PASSWORD }}
          MT5_SERVER: ${{ secrets.MT5_SERVER }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: python xag_main.py

      - name: "[Hesap 2 - 10000 USD] AUDNZD botunu calistir"
        continue-on-error: true
        env:
          METAAPI_TOKEN: ${{ secrets.METAAPI_TOKEN }}
          MT5_LOGIN: ${{ secrets.MT5_LOGIN_2 }}
          MT5_PASSWORD: ${{ secrets.MT5_PASSWORD_2 }}
          MT5_SERVER: ${{ secrets.MT5_SERVER_2 }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          HESAP_ETIKETI: "_hesap2"
        run: python audnzd_main.py

      - name: Pozisyon durumunu commitle
        if: always()
        run: |
          git config user.name "xau-xag-bot"
          git config user.email "bot@users.noreply.github.com"
          git add pozisyon_durumu.json pozisyon_durumu_hesap2.json 2>/dev/null || true
          git diff --cached --quiet || git commit -m "Pozisyon durumu guncellendi"
          git push
